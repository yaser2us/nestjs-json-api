import { testSimpleAccessControl } from './access-controlled-get-all';

describe('Access Controlled getAll', () => {
  it('should test mock access function', () => {
    testSimpleAccessControl();
    // Check console output for now
  });
});